'use client';
import { DirectDealForm } from './DirectDealForm';

// Keep the business and personal entry points on the same structured form.
export function DirectDealComposer() {
  return <DirectDealForm />;
}
